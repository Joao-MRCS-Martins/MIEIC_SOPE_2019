#include <stdio.h>
#include <stdlib.h>
#include <string.h>

int main(int argc, char ** argv,char * envp[]) {
    char* user = strncpy(user,envp[4])
    printf("Hello %s!\n",strncpy(envp[4]);
    return 0;
}
